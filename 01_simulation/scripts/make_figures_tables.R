make_figures_tables <- function(paths, nsim = 10L, pilot = TRUE) {
  make_figures(paths = paths, nsim = nsim, pilot = pilot)
}

make_multicov_figures_tables <- function(paths, nsim = 10L, pilot = TRUE) {
  make_multicov_figures(paths = paths, nsim = nsim, pilot = pilot)
}

make_nonlinear_figures_tables <- function(paths, nsim = 10L, pilot = TRUE) {
  make_nonlinear_figures(paths = paths, nsim = nsim, pilot = pilot)
}

make_ripd_truncation_figures_tables <- function(paths, nsim = 10L, pilot = TRUE) {
  make_ripd_truncation_figures(paths = paths, nsim = nsim, pilot = pilot)
}
